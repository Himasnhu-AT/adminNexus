# docker compose -f docker-compose-test-db.yml up -d

services:
  mysql:
    image: mysql:latest
    environment:
      MYSQL_ROOT_PASSWORD: rootpassword
      MYSQL_DATABASE: mydatabase # Add this line
    ports:
      - 3306:3306
    volumes:
      - mysql_data:/var/lib/mysql

  postgres:
    image: postgres:latest
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: rootpassword
      POSTGRES_DB: postgres # Add this line
    ports:
      - 5432:5432
    volumes:
      - postgres_data:/var/lib/postgresql/data

  mongo:
    image: mongo:latest
    environment:
      MONGO_INITDB_DATABASE: mydatabase # Add this line
    ports:
      - 27017:27017
    volumes:
      - mongo_data:/data/db

volumes:
  mysql_data:
  postgres_data:
  mongo_data:
# - MySQL: `mysql://root:rootpassword@localhost:3306`
# - PostgreSQL: `postgresql://postgres:rootpassword@localhost:5432`
# - MongoDB: `mongodb://localhost:27017`
